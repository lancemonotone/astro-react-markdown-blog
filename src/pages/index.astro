---
const name = "Rus";
const style = "purple";
const response = await fetch(
  "https://jsonplaceholder.typicode.com/users?limit=5"
);
const users = await response.json();
const visible = false;
---

<div class="container">
  <h1 class={style}>Hello {name}</h1>

  <ul>
    {
      users.map((user) => (
        <li>
          {Object.keys(user).map((key) => {
            if (typeof user[key] === "object") {
              let obj = user[key];
              return (
                <div>
                  <h3>{key}</h3>{" "}
                  {Object.keys(obj).map((key) => {
                    return (
                      <div>
                        {key}: {obj[key]}
                      </div>
                    );
                  })}
                </div>
              );
            } else {
              return (
                <div>
                  <h2>{key}</h2> {user[key]}
                </div>
              );
            }
          })}
        </li>
      ))
    }
  </ul>

  {visible ? <p>Visible</p> : <p>Not Visible</p>}
</div>

<style lang="scss" scoped>
  // :root darker mode styling
    :root {
    --main-bg-color: #1e1e1e;
     // not quite white
    --main-color: #f2e6f2;
    // what is a complimentary color to  #800080
    --main-border-color: #cc99cc;
    }

  // html body reset
  html,
  body {
    background-color: var(--main-bg-color);
    color: var(--main-color);
    margin: 0;
    padding: 0;
    font-family: sans-serif;
  }

  //  heading reset
  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    margin: 0;
    padding: 0;
  }
  .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-gap: 1rem;
    padding: 1rem;
    border: 1px solid var(--main-border-color);
    border-radius: 5px;
    h1 {
      color: var(--main-color);
      // after element
      &:after {
        content: "!";
      }
    }
  }
  .purple {
    background-color: #4d004d;
    color: var(--main-color);
    padding: 1rem;
    border: 1px solid var(--main-border-color);
    border-radius: 5px;
  }
  //  bootstrap ul styling
  ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  li {
    background-color: var(--main-bg-color);
    padding: 1rem;
    border: 1px solid var(--main-border-color);
    border-radius: 5px;
    margin-block-end: 1rem;
    &:last-child {
      margin-block-end: 0;
    }
    div {
      margin-block-end: 1rem;
      div {
        padding-inline-start: 1rem;
        margin-block: 0.5rem;
        &:last-child {
          margin-block-end: 0;
        }
      }
      &:last-child {
        margin-block-end: 0;
      }
    }
  }
</style>
